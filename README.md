# 👗 Fashion Agentic AI System

<div align="center">

![Python](https://img.shields.io/badge/Python-3.9+-blue.svg)
![FastAPI](https://img.shields.io/badge/FastAPI-0.100+-green.svg)
![React](https://img.shields.io/badge/React-18-blue.svg)
![PyTorch](https://img.shields.io/badge/PyTorch-2.1+-orange.svg)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)

**패션 Agentic AI 가상 피팅 시스템 - 지능형 의류 생성 및 3D 변환 플랫폼**

[개요](#-프로젝트-정의) • [특징](#-주요-특징) • [설치](#-설치-방법) • [사용법](#-사용-방법) • [아키텍처](#-시스템-아키텍처)

</div>

---

## 📋 프로젝트 정의

**Fashion Agentic AI System**은 ChatGarment 모델을 기반으로 하는 혁신적인 **Agentic AI 가상 피팅 시스템**입니다. 

이 시스템은 단순한 기능 집합이 아닌, **지능형 에이전트(Intelligent Agent)**가 사용자의 복합적인 의도를 인식하고, 최적의 작업 계획을 판단하며, 필요한 기술 모듈을 자율적으로 사용하여 결과를 생성하는 **자율적 문제 해결 프로세스**를 구현합니다.

### 핵심 목표

1. **2D 이미지에서 3D 의류 모델 자동 생성**: 이미지나 텍스트 입력을 통해 사실적인 3D 의류 모델을 생성
2. **지능형 작업 오케스트레이션**: Agentic AI 프레임워크를 통해 복잡한 작업을 자동화하고 지능적으로 처리
3. **멀티모달 입력 처리**: 텍스트와 이미지를 동시에 처리하여 사용자의 의도를 정확히 파악
4. **가상 피팅 시뮬레이션**: 생성된 3D 의류를 3D 아바타에 피팅하여 시각적으로 확인

### 기술적 특징

- **Agentic AI Framework**: "인식(Perception) → 판단(Judgment) → 행동(Action)"의 3단계 프로세스
- **멀티 에이전트 아키텍처**: Agent 1 (오케스트레이션) + Agent 2 (실행 계획 생성)
- **LLaVA 기반 의류 생성**: Vision-Language 모델을 활용한 의류 분석 및 생성
- **2D → 3D 자동 변환**: GarmentCodeRC를 통한 물리 기반 시뮬레이션

---

## ✨ 주요 특징

### 1. 🧠 Agentic AI Framework

**지능형 에이전트 기반 시스템**으로, 사용자의 요청을 분석하고 자동으로 최적의 작업 계획을 수립합니다.

- **Agent 1 (Agent Runtime)**: 종합 감독 에이전트로 전체 프로세스를 오케스트레이션
- **Agent 2 (F.LLM)**: 작업 지시 전문가로 구체적인 실행 계획을 JSON 형식으로 생성
- **자기 수정 루프**: 결과를 검증하고 필요 시 자동으로 재시도하여 정확도 향상
- **도구 레지스트리 시스템**: 플러그인 방식으로 새로운 기능을 쉽게 추가 가능

### 2. 🎨 멀티모달 입력 처리

**텍스트와 이미지를 동시에 처리**하여 사용자의 의도를 정확히 파악합니다.

- **이미지 기반 분석**: 의류 이미지에서 스타일, 색상, 재질 등을 자동 분석
- **텍스트 기반 생성**: 텍스트 설명만으로도 새로운 의류 생성 가능
- **하이브리드 입력**: 이미지와 텍스트를 결합하여 더 정확한 결과 생성

### 3. 🔄 2D → 3D 자동 변환 파이프라인

**2D 의류 패턴을 사실적인 3D 모델로 자동 변환**합니다.

- **ChatGarment 통합**: LLaVA 기반 모델로 이미지에서 GarmentCode JSON 패턴 생성
- **GarmentCodeRC 변환**: JSON 패턴을 3D 메시로 변환
- **물리 시뮬레이션**: 실제 의류 드레이핑을 시뮬레이션하여 자연스러운 형태 생성
- **고품질 렌더링**: 3D 모델을 이미지로 렌더링하여 시각화

### 4. 🖼️ 3D 인터랙티브 뷰어

**React + Three.js 기반 웹 인터페이스**로 3D 모델을 실시간으로 확인할 수 있습니다.

- **3D 모델 뷰어**: Three.js를 사용한 고품질 3D 시각화
- **인터랙티브 조작**: 회전, 확대/축소, 이동 등 자유로운 조작
- **실시간 상태 표시**: 처리 진행 상황을 실시간으로 표시
- **반응형 UI**: 모바일 및 데스크톱 환경 모두 지원

### 5. 🔍 InternVL2 통합

**강력한 Vision-Language 모델(VLM)**을 활용하여 이미지 이해 및 분석 능력을 강화합니다.

- **멀티모달 VLM**: 이미지와 텍스트를 동시에 처리
- **자동 디바이스 감지**: CUDA/CPU 자동 감지 및 최적화
- **규칙 기반 폴백**: 모델이 없어도 기본 기능 제공

### 6. 🚀 FastAPI 기반 API 서버

**RESTful API**를 통해 외부 서비스와 쉽게 연동할 수 있습니다.

- **비동기 처리**: FastAPI의 비동기 처리로 높은 성능
- **자동 API 문서**: Swagger UI를 통한 자동 문서화
- **CORS 지원**: 크로스 오리진 요청 지원
- **마이크로서비스 아키텍처**: 독립적인 서비스로 확장 가능

### 7. 📚 RAG (Retrieval-Augmented Generation)

**지식 검색 및 보강**을 통해 에이전트의 판단 정확도를 높입니다.

- **Mock RAG**: PoC 단계에서 기본 지식 베이스 제공
- **벡터 RAG 준비**: 향후 Vector DB 기반 검색 지원 예정

---

## 💡 작동 원리

### 전체 시스템 흐름

```
[사용자 입력 (이미지 + 텍스트)]
    ↓
[프론트엔드 (React)]
    ↓
[API 서버 (FastAPI)]
    ↓
[Agent Runtime (Agent 1)]
    ├─ 인식 (Perception): 사용자 의도 분석
    ├─ 판단 (Judgment): 추상적 계획 수립
    └─ 행동 (Action): 도구 실행 오케스트레이션
    ↓
[F.LLM (Agent 2) - InternVL2-8B]
    ├─ 추상적 계획을 구체적 실행 계획으로 변환
    ├─ JSON 형식 실행 계획 생성
    └─ RAG를 통한 지식 보강
    ↓
[도구 실행]
    ├─ Extensions (2D→3D 변환)
    │   ├─ ChatGarment: 이미지 분석 및 패턴 생성
    │   ├─ GarmentCodeRC: 3D 메시 생성
    │   └─ 렌더링: 이미지 생성
    │
    └─ Functions (상품 검색)
    ↓
[결과 검증 및 재시도 루프]
    ↓
[최종 결과 반환]
    ↓
[프론트엔드 시각화 (3D 뷰어)]
```

### Agent 간 상호작용

```
Agent 1 (Agent Runtime) - 종합 감독
    │
    ├─ 사용자 요청 분석
    ├─ 추상적 계획 수립
    │
    └─→ Agent 2 (F.LLM) - 작업 지시 전문가
            │
            ├─ InternVL2 모델을 통한 분석
            ├─ 구체적 실행 계획 생성 (JSON)
            │
            └─→ 도구 실행
                    │
                    ├─ Extensions (2D→3D)
                    └─ Functions (검색)
```

### 상세 작동 원리

#### 1. 인식 단계 (Perception)

**Agent Runtime**이 사용자의 요청을 분석합니다:

```python
# 사용자 입력 분석
user_intent = {
    "type": "3d_generation",  # 또는 "garment_recommendation"
    "input_data": {
        "text": "이 티셔츠를 3D로 만들어줘",
        "image_path": "/path/to/image.jpg"
    }
}
```

#### 2. 판단 단계 (Judgment)

**Agent 1**이 추상적 계획을 수립하고, **Agent 2**가 구체적 실행 계획을 생성합니다:

```python
# Agent 1: 추상적 계획
abstract_plan = {
    "plan_type": "3d_generation",
    "goal": "이미지에서 3D 의류 모델 생성",
    "steps": [
        "이미지 분석",
        "패턴 생성",
        "3D 변환",
        "렌더링"
    ]
}

# Agent 2: 구체적 실행 계획
execution_plan = {
    "plan_id": "plan_001",
    "steps": [
        {
            "tool": "extensions_2d_to_3d",
            "action": "analyze_and_generate",
            "parameters": {
                "image_path": "/path/to/image.jpg",
                "output_dir": "/outputs"
            }
        }
    ]
}
```

#### 3. 행동 단계 (Action)

**도구(Tools)**를 실행하여 실제 작업을 수행합니다:

1. **ChatGarment 분석**: 이미지에서 의류 정보 추출
2. **패턴 생성**: GarmentCode JSON 형식으로 2D 패턴 생성
3. **3D 변환**: GarmentCodeRC를 통해 3D 메시 생성
4. **물리 시뮬레이션**: 의류 드레이핑 시뮬레이션
5. **렌더링**: 최종 이미지 생성

#### 4. 자기 수정 루프 (Self-Correction Loop)

결과를 검증하고 필요 시 재시도합니다:

```python
# 결과 검증
if result["status"] == "error":
    # 재시도 로직
    retry_plan = adjust_plan(execution_plan, result["error"])
    result = execute_plan(retry_plan)
```

---

## 📁 파일 디렉토리 구조

### 전체 디렉토리 구조

```
Fashion-Agentic-AI-System_01/
├── agentic_system/                 # Agentic AI 시스템 (메인 시스템)
│   ├── api/                        # FastAPI 서버
│   │   ├── __init__.py
│   │   └── main.py                 # 메인 API 엔드포인트
│   │
│   ├── core/                       # 핵심 컴포넌트
│   │   ├── __init__.py
│   │   ├── agent_runtime.py        # Agent 1 (오케스트레이션)
│   │   ├── f_llm.py                # Agent 2 (실행 계획 생성)
│   │   ├── memory.py               # 메모리 관리
│   │   └── custom_ui.py            # UI 컴포넌트 인터페이스
│   │
│   ├── tools/                      # 도구 (Extensions & Functions)
│   │   ├── __init__.py
│   │   ├── extensions.py            # 2D→3D 변환 도구
│   │   ├── extensions_service.py    # 서비스 통합 확장
│   │   ├── chatgarment_integration.py  # ChatGarment 통합
│   │   └── functions.py            # 상품 검색 도구
│   │
│   ├── data_stores/                # RAG 데이터 저장소
│   │   ├── __init__.py
│   │   ├── rag.py                  # Mock RAG 구현
│   │   └── rag_vector.py           # Vector RAG (향후 확장)
│   │
│   ├── models/                     # AI 모델 래퍼
│   │   ├── __init__.py
│   │   └── internvl2_wrapper.py    # InternVL2-8B 래퍼
│   │
│   ├── frontend/                   # React 프론트엔드
│   │   ├── src/
│   │   │   ├── App.jsx             # 메인 앱 컴포넌트
│   │   │   ├── main.jsx            # 진입점
│   │   │   └── components/
│   │   │       ├── ImageUpload.jsx  # 이미지 업로드
│   │   │       ├── StatusBar.jsx    # 상태 표시
│   │   │       ├── ResultViewer.jsx # 결과 표시
│   │   │       └── ModelViewer.jsx  # 3D 모델 뷰어
│   │   ├── index.html
│   │   ├── package.json
│   │   └── vite.config.js
│   │
│   ├── chatgarment_service/        # ChatGarment 마이크로서비스
│   │   ├── main.py                 # FastAPI 서비스
│   │   ├── requirements.txt
│   │   └── start_service.sh
│   │
│   ├── requirements.txt            # Python 의존성
│   └── README.md                   # Agentic 시스템 문서
│
├── ChatGarment/                    # ChatGarment 모델 (LLaVA 기반)
│   ├── llava/                      # LLaVA 모델 구현
│   │   ├── __init__.py
│   │   ├── garment_utils_v2.py     # 의류 생성 유틸리티
│   │   ├── garmentcode_utils.py    # GarmentCode 통합
│   │   ├── garmentcodeRC_utils.py  # GarmentCodeRC 통합
│   │   ├── mm_utils.py             # 멀티모달 유틸리티
│   │   └── model/                   # 모델 아키텍처
│   │       ├── llava_arch.py
│   │       └── ...
│   │
│   ├── scripts/                    # 실행 스크립트
│   │   ├── evaluate_garment_v2_imggen_2step.sh
│   │   ├── evaluate_garment_v2_textgen.sh
│   │   └── evaluate_garment_v2_demo_edit.sh
│   │
│   ├── example_data/               # 예제 데이터
│   │   ├── example_imgs/           # 예제 이미지
│   │   ├── example_jsons/           # 예제 JSON
│   │   └── example_sewing_patterns/ # 예제 패턴
│   │
│   ├── docs/                       # 문서
│   │   ├── Installation.md
│   │   ├── postprocess.md
│   │   └── prompts/                # 프롬프트 템플릿
│   │
│   ├── run_garmentcode_sim.py      # 3D 시뮬레이션 실행
│   ├── pyproject.toml
│   ├── LICENSE
│   └── README.md
│
├── GarmentCodeRC/                  # GarmentCode 라이브러리 (2D→3D 변환)
│   ├── pygarment/                  # 핵심 라이브러리 (Python 바인딩)
│   │   ├── garmentcode/            # 패턴 생성 엔진
│   │   │   ├── base.py
│   │   │   ├── component.py
│   │   │   ├── edge.py
│   │   │   ├── panel.py
│   │   │   └── ...
│   │   │
│   │   ├── meshgen/                # 3D 메시 생성
│   │   │   ├── boxmeshgen.py
│   │   │   ├── simulation.py
│   │   │   └── render/
│   │   │
│   │   └── pattern/                # 패턴 렌더링
│   │
│   ├── assets/                     # 자산
│   │   ├── garment_programs/       # 의류 프로그램 (Python)
│   │   ├── bodies/                 # 바디 모델 (OBJ, YAML)
│   │   ├── design_params/          # 디자인 파라미터
│   │   └── Patterns/               # 예제 패턴 JSON
│   │
│   ├── gui/                        # GUI 인터페이스
│   │   ├── callbacks.py
│   │   └── gui_pattern.py
│   │
│   ├── pyproject.toml
│   ├── setup.cfg
│   ├── LICENSE
│   └── ReadMe.md
│
├── chatgarment_service/            # 독립 ChatGarment 서비스
│   ├── main.py
│   ├── requirements.txt
│   └── README.md
│
├── checkpoints/                    # AI 모델 체크포인트 (GitHub 제외)
│   ├── llava-v1.5-7b/              # LLaVA 기본 모델
│   └── try_7b_lr1e_4_v3_garmentcontrol_4h100_v4_final/
│       └── pytorch_model.bin       # ChatGarment 파인튜닝 모델
│
├── model/                          # InternVL2 모델 (GitHub 제외)
│   └── InternVL2_8B/              # InternVL2-8B 모델 파일
│
├── outputs/                        # 시스템 출력 (GitHub 제외)
│   ├── patterns/                   # 2D 패턴 (JSON)
│   ├── 3d_models/                  # 3D 모델 (OBJ, GLB)
│   └── renders/                    # 렌더링 이미지 (PNG)
│
├── uploads/                        # 사용자 업로드 파일 (GitHub 제외)
│
├── .gitignore                      # Git 제외 파일 목록
├── PROJECT_STRUCTURE_ANALYSIS.md   # 프로젝트 구조 분석 문서
├── CODE_REVIEW_REPORT.md           # 코드 검토 보고서
├── GITHUB_UPLOAD_LIST.md           # GitHub 업로드 파일 목록
└── README.md                       # 현재 문서
```

### 주요 디렉토리 설명

#### `agentic_system/` - Agentic AI 메인 시스템

- **`api/`**: FastAPI 기반 REST API 서버
  - `main.py`: 메인 API 엔드포인트 (`/api/v1/request`, `/health` 등)
  
- **`core/`**: 핵심 컴포넌트
  - `agent_runtime.py`: Agent 1 (오케스트레이션 엔진)
  - `f_llm.py`: Agent 2 (실행 계획 생성, InternVL2 통합)
  - `memory.py`: 세션 메모리 및 대화 기록 관리
  - `custom_ui.py`: UI 컴포넌트 인터페이스

- **`tools/`**: 에이전트가 사용하는 도구
  - `extensions.py`: 2D→3D 변환 도구 (ChatGarment + GarmentCodeRC 통합)
  - `chatgarment_integration.py`: ChatGarment 파이프라인 래퍼
  - `functions.py`: 상품 검색 및 매칭 도구

- **`data_stores/`**: RAG 데이터 저장소
  - `rag.py`: Mock RAG 구현 (PoC 단계)
  - `rag_vector.py`: Vector RAG (향후 확장)

- **`models/`**: AI 모델 래퍼
  - `internvl2_wrapper.py`: InternVL2-8B 모델 래퍼

- **`frontend/`**: React 프론트엔드
  - React 18 + Vite + Three.js 기반

#### `ChatGarment/` - LLaVA 기반 의류 생성 모델

- **`llava/`**: LLaVA 모델 구현
  - Vision-Language 모델을 사용한 이미지 분석 및 패턴 생성
  
- **`scripts/`**: 모델 실행 스크립트
  - 이미지 기반 생성, 텍스트 기반 생성, 편집 기능

#### `GarmentCodeRC/` - 2D→3D 변환 라이브러리

- **`pygarment/`**: Python 바인딩 모듈
  - C++ 기반 라이브러리의 Python 인터페이스
  - 패턴 생성, 3D 메시 생성, 물리 시뮬레이션

---

## 🛠️ 설치 방법

### 1. 전제 조건 (Prerequisites)

다음 소프트웨어가 설치되어 있어야 합니다:

- **Python 3.9 이상**: 프로젝트의 주요 개발 언어
- **Git**: 소스 코드 클론을 위해 필요
- **Node.js 18+ & npm**: 프론트엔드 개발을 위해 필요 (선택사항)
- **CUDA 11.8+ (선택사항)**: GPU 가속을 위해 필요 (모델 추론 속도 향상)
- **Visual Studio Build Tools (Windows)**: C++ 확장 모듈 컴파일을 위해 필요 (GarmentCodeRC)

### 2. 저장소 클론

```bash
# GitHub 저장소 클론
git clone https://github.com/heosanghun/Fashion-Agentic-AI-System_01.git

# 프로젝트 디렉토리로 이동
cd Fashion-Agentic-AI-System_01
```

### 3. Python 가상 환경 설정

```bash
# 가상 환경 생성
python -m venv venv

# 가상 환경 활성화
# Windows
.\venv\Scripts\activate

# Linux/macOS
source venv/bin/activate
```

### 4. Python 의존성 설치

프로젝트의 각 컴포넌트별로 의존성을 설치합니다:

```bash
# 1. Agentic AI 시스템 의존성
pip install -r agentic_system/requirements.txt

# 2. ChatGarment 서비스 의존성
pip install -r agentic_system/chatgarment_service/requirements.txt

# 3. ChatGarment 모델 의존성 (ChatGarment 디렉토리에서)
cd ChatGarment
pip install -e .
cd ..

# 4. GarmentCodeRC 의존성 (GarmentCodeRC 디렉토리에서)
cd GarmentCodeRC
pip install -e .
cd ..
```

**주요 의존성 패키지:**
- `torch>=2.1.2`: PyTorch (AI 모델 실행)
- `transformers>=4.37.2`: Hugging Face Transformers
- `fastapi>=0.100.0`: FastAPI (REST API 서버)
- `uvicorn`: ASGI 서버
- `pillow`: 이미지 처리
- `numpy`: 수치 연산
- `pydantic`: 데이터 검증

### 5. 모델 파일 다운로드

⚠️ **중요**: 모델 파일은 용량이 크기 때문에 GitHub에 포함되지 않습니다. 별도로 다운로드해야 합니다.

#### 5.1 ChatGarment 체크포인트

```bash
# checkpoints 디렉토리 생성
mkdir -p checkpoints/try_7b_lr1e_4_v3_garmentcontrol_4h100_v4_final

# ChatGarment 파인튜닝 모델 다운로드
# (다운로드 링크는 프로젝트 관리자에게 문의)
# 다운로드 후 다음 위치에 저장:
# checkpoints/try_7b_lr1e_4_v3_garmentcontrol_4h100_v4_final/pytorch_model.bin
```

#### 5.2 LLaVA 기본 모델

```bash
# LLaVA 1.5 7B 모델 다운로드
# Hugging Face에서 다운로드:
# https://huggingface.co/liuhaotian/llava-v1.5-7b

# 다음 위치에 저장:
# checkpoints/llava-v1.5-7b/
```

#### 5.3 InternVL2-8B 모델

```bash
# InternVL2-8B 모델 다운로드
# Hugging Face에서 다운로드:
# https://huggingface.co/OpenGVLab/InternVL2-8B

# 다음 위치에 저장:
# model/InternVL2_8B/
```

**참고**: 모델 파일은 총 수십 GB에 달하므로 충분한 디스크 공간이 필요합니다.

### 6. 환경 변수 설정 (선택사항)

프로젝트 루트에 `.env` 파일을 생성하여 환경 변수를 설정할 수 있습니다:

```bash
# .env 파일 생성 (프로젝트 루트)
touch .env
```

`.env` 파일 내용 예시:

```ini
# ChatGarment 서비스 URL
CHATGARMENT_SERVICE_URL=http://localhost:9000
USE_CHATGARMENT_SERVICE=true

# 디바이스 설정
DEVICE=cuda  # 또는 "cpu"

# 출력 디렉토리
OUTPUT_DIR=./outputs

# API 서버 설정
API_HOST=0.0.0.0
API_PORT=8000
```

### 7. 프론트엔드 의존성 설치 (선택사항)

프론트엔드를 사용하려면 Node.js 의존성을 설치해야 합니다:

```bash
cd agentic_system/frontend

# 의존성 설치
npm install

# 또는 yarn 사용
yarn install
```

---

## 🚀 사용 방법

### 1. 서비스 실행

#### 1.1 ChatGarment 서비스 실행

ChatGarment 서비스는 독립적으로 실행할 수 있습니다:

```bash
cd agentic_system/chatgarment_service

# 서비스 실행
python main.py

# 또는 uvicorn 직접 실행
uvicorn main:app --host 0.0.0.0 --port 9000 --reload
```

서비스가 실행되면 다음 URL에서 접근 가능합니다:
- **API 문서**: http://localhost:9000/docs
- **헬스 체크**: http://localhost:9000/health

#### 1.2 Agentic AI 시스템 API 서버 실행

메인 API 서버를 실행합니다:

```bash
cd agentic_system

# API 서버 실행
python -m uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload

# 또는 직접 실행
python start_api_server.py
```

서비스가 실행되면:
- **API 문서**: http://localhost:8000/docs
- **헬스 체크**: http://localhost:8000/health

#### 1.3 프론트엔드 실행 (선택사항)

React 프론트엔드를 실행합니다:

```bash
cd agentic_system/frontend

# 개발 서버 실행
npm run dev

# 또는 yarn 사용
yarn dev
```

프론트엔드는 http://localhost:5173 에서 실행됩니다.

### 2. API 사용 예제

#### 2.1 이미지 기반 3D 생성 요청

```python
import requests

# API 엔드포인트
url = "http://localhost:8000/api/v1/request"

# 요청 데이터
payload = {
    "input_data": {
        "text": "이 티셔츠를 3D로 만들어줘",
        "image_path": "/path/to/image.jpg"
    },
    "session_id": "user_123"
}

# 요청 전송
response = requests.post(url, json=payload)
result = response.json()

print(result)
```

#### 2.2 ChatGarment 서비스 직접 호출

```python
import requests

# ChatGarment 서비스 엔드포인트
url = "http://localhost:9000/api/v1/process"

# 이미지 파일 업로드
with open("image.jpg", "rb") as f:
    files = {"image": f}
    data = {"text": "이미지 분석 및 3D 생성"}
    
    response = requests.post(url, files=files, data=data)
    result = response.json()
    
    print(result)
```

### 3. 스크립트 실행

#### 3.1 ChatGarment 모델 직접 실행

```bash
cd ChatGarment

# 이미지 기반 생성 (2단계)
./scripts/v1_5/evaluate_garment_v2_imggen_2step.sh example_data/example_imgs/

# 텍스트 기반 생성
./scripts/v1_5/evaluate_garment_v2_textgen.sh example_data/example_jsons/example_textgen_prompts.json

# 의류 편집
./scripts/v1_5/evaluate_garment_v2_demo_edit.sh example_data/example_jsons/example_edit_prompts.json
```

#### 3.2 3D 시뮬레이션 실행

```bash
# 2D 패턴을 3D로 변환
cd ChatGarment
python run_garmentcode_sim.py --all_paths_json outputs/pattern_output/
```

---

## 🏗️ 시스템 아키텍처

### 전체 아키텍처 다이어그램

```
┌─────────────────────────────────────────────────────────────┐
│                    사용자 인터페이스                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   React UI   │  │   REST API   │  │   Web UI     │      │
│  │  (Frontend)  │  │   (Client)   │  │  (Browser)   │      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
└─────────┼──────────────────┼──────────────────┼─────────────┘
          │                  │                  │
          └──────────────────┼──────────────────┘
                             │
          ┌──────────────────▼──────────────────┐
          │      FastAPI API Server             │
          │    (agentic_system/api/main.py)     │
          └──────────────────┬──────────────────┘
                             │
          ┌──────────────────▼──────────────────┐
          │      Agent Runtime (Agent 1)         │
          │  (agentic_system/core/agent_runtime) │
          │  - 인식 (Perception)                │
          │  - 판단 (Judgment)                   │
          │  - 행동 (Action)                     │
          └──────────────────┬──────────────────┘
                             │
          ┌──────────────────▼──────────────────┐
          │      F.LLM (Agent 2)                │
          │  (agentic_system/core/f_llm)         │
          │  - InternVL2-8B 통합                 │
          │  - 실행 계획 생성                     │
          └──────────────────┬──────────────────┘
                             │
          ┌──────────────────▼──────────────────┐
          │         Tools (도구)                 │
          │  ┌──────────────┐  ┌──────────────┐ │
          │  │ Extensions   │  │  Functions   │ │
          │  │ (2D→3D)      │  │  (검색)      │ │
          │  └──────┬───────┘  └──────┬───────┘ │
          └─────────┼──────────────────┼─────────┘
                    │                  │
    ┌───────────────┼──────────────────┼───────────────┐
    │               │                  │               │
┌───▼──────┐  ┌─────▼──────┐  ┌───────▼──────┐        │
│ChatGarment│  │GarmentCodeRC│  │  RAG Store   │        │
│  (LLaVA)  │  │  (2D→3D)   │  │  (지식)      │        │
└───────────┘  └─────────────┘  └──────────────┘        │
```

### 컴포넌트별 상세 설명

#### Agent Runtime (Agent 1)

**역할**: 종합 감독 에이전트

**주요 기능**:
- 사용자 요청 분석 및 의도 파악
- 추상적 작업 계획 수립
- Agent 2에게 계획 전달 및 결과 수신
- 도구 실행 오케스트레이션
- 자기 수정 루프 관리 (결과 검증 및 재시도)

**코드 위치**: `agentic_system/core/agent_runtime.py`

#### F.LLM (Agent 2)

**역할**: 작업 지시 전문가 에이전트

**주요 기능**:
- 추상적 계획을 구체적 실행 계획으로 변환
- InternVL2-8B 모델을 통한 멀티모달 분석
- JSON 형식 실행 계획 생성
- RAG를 통한 지식 보강

**코드 위치**: `agentic_system/core/f_llm.py`

#### Extensions Tool (2D→3D 변환)

**역할**: 2D 이미지를 3D 모델로 변환

**주요 기능**:
- ChatGarment를 통한 이미지 분석 및 패턴 생성
- GarmentCodeRC를 통한 3D 메시 생성
- 물리 시뮬레이션 실행
- 렌더링 이미지 생성

**코드 위치**: `agentic_system/tools/extensions.py`

---

## 🔧 설정 및 구성

### 환경 변수

프로젝트는 다음과 같은 환경 변수를 지원합니다:

| 변수명 | 설명 | 기본값 |
|--------|------|--------|
| `CHATGARMENT_SERVICE_URL` | ChatGarment 서비스 URL | `http://localhost:9000` |
| `USE_CHATGARMENT_SERVICE` | ChatGarment 서비스 사용 여부 | `true` |
| `DEVICE` | 실행 디바이스 (cuda/cpu) | `cuda` (CUDA 사용 가능 시) |
| `OUTPUT_DIR` | 출력 디렉토리 경로 | `./outputs` |
| `API_HOST` | API 서버 호스트 | `0.0.0.0` |
| `API_PORT` | API 서버 포트 | `8000` |

### 설정 파일

#### `.gitignore`

다음 파일/디렉토리는 Git 추적에서 제외됩니다:

- `checkpoints/`: 모델 체크포인트
- `model/InternVL2_8B/*.safetensors`: 모델 가중치 파일
- `outputs/`: 출력 파일
- `uploads/`: 사용자 업로드 파일
- `__pycache__/`: Python 캐시
- `node_modules/`: Node.js 패키지
- `*.log`: 로그 파일

---

## 🧪 테스트

### 단위 테스트

```bash
# Agentic 시스템 테스트
cd agentic_system
python -m pytest tests/

# ChatGarment 통합 테스트
python test_chatgarment_integration.py

# 전체 시스템 테스트
python test_full_integration.py
```

### API 테스트

```bash
# API 서버 테스트
cd agentic_system
python test_api_server.py

# 간단한 요청 테스트
python test_simple_request.py
```

---

## 📊 성능 및 요구사항

### 하드웨어 요구사항

#### 최소 사양
- **CPU**: 4코어 이상
- **RAM**: 16GB 이상
- **디스크**: 100GB 이상 (모델 파일 포함)
- **GPU**: 선택사항 (CPU 모드 가능)

#### 권장 사양
- **CPU**: 8코어 이상
- **RAM**: 32GB 이상
- **디스크**: 200GB 이상 (모델 파일 포함)
- **GPU**: NVIDIA GPU with CUDA 11.8+ (16GB VRAM 권장)

### 성능 지표

- **이미지 분석**: 약 2-5초 (GPU 사용 시)
- **패턴 생성**: 약 5-10초 (GPU 사용 시)
- **3D 변환**: 약 10-30초 (시뮬레이션 시간 포함)
- **전체 파이프라인**: 약 20-50초 (GPU 사용 시)

---

## 🤝 기여 가이드

기여를 환영합니다! 버그 리포트, 기능 제안, 코드 개선 등 어떤 형태의 기여라도 좋습니다.

### 기여 방법

1. **저장소 포크 (Fork)**
   ```bash
   # GitHub에서 저장소를 포크합니다
   ```

2. **브랜치 생성**
   ```bash
   git checkout -b feature/AmazingFeature
   ```

3. **변경사항 커밋**
   ```bash
   git commit -m 'Add some AmazingFeature'
   ```

4. **브랜치에 푸시**
   ```bash
   git push origin feature/AmazingFeature
   ```

5. **Pull Request 생성**
   - GitHub에서 Pull Request를 생성합니다
   - 변경사항을 자세히 설명합니다

### 코딩 스타일

- **Python**: PEP 8 스타일 가이드 준수
- **JavaScript**: ESLint 설정 준수
- **커밋 메시지**: 명확하고 간결하게 작성

---

## 📄 라이선스

이 프로젝트는 MIT 라이선스 하에 배포됩니다. 자세한 내용은 `LICENSE` 파일을 참조하십시오.

---

## 🙏 감사의 글

이 프로젝트는 다음 오픈소스 프로젝트를 기반으로 합니다:

- **[LLaVA](https://github.com/haotian-liu/LLaVA)**: Vision-Language 모델
- **[ChatGarment](https://chatgarment.github.io/)**: 의류 생성 및 분석 모델
- **[GarmentCode](https://github.com/maria-korosteleva/GarmentCode)**: 의류 패턴 생성 라이브러리
- **[InternVL2](https://github.com/OpenGVLab/InternVL2)**: Vision-Language 모델
- **[FastAPI](https://fastapi.tiangolo.com/)**: 현대적인 웹 프레임워크
- **[React](https://react.dev/)**: 사용자 인터페이스 라이브러리
- **[Three.js](https://threejs.org/)**: 3D 그래픽 라이브러리

---

## 📞 문의 및 지원

### 개발자 정보

- **GitHub**: [heosanghun](https://github.com/heosanghun)
- **저장소**: [Fashion-Agentic-AI-System_01](https://github.com/heosanghun/Fashion-Agentic-AI-System_01)

### 문제 리포트

버그를 발견하거나 기능 제안이 있으시면 [Issues](https://github.com/heosanghun/Fashion-Agentic-AI-System_01/issues)에 등록해주세요.

### 추가 문서

- [프로젝트 구조 분석](PROJECT_STRUCTURE_ANALYSIS.md)
- [코드 검토 보고서](CODE_REVIEW_REPORT.md)
- [GitHub 업로드 가이드](GITHUB_UPLOAD_GUIDE.md)

---

## 🗺️ 로드맵

### PoC 단계 (현재)
- ✅ Agentic AI 프레임워크 구현
- ✅ ChatGarment 통합
- ✅ 2D→3D 변환 파이프라인
- ✅ 기본 웹 UI

### Pilot 단계 (향후)
- [ ] Vector RAG 통합
- [ ] 실제 상품 검색 API 연동
- [ ] 더 많은 의류 유형 지원
- [ ] 성능 최적화

### Production 단계 (장기)
- [ ] 클라우드 배포
- [ ] 대규모 사용자 지원
- [ ] 실시간 협업 기능
- [ ] 모바일 앱 지원

---

<div align="center">

**⭐ 이 프로젝트가 도움이 되었다면 Star를 눌러주세요! ⭐**

Made with ❤️ by heosanghun

</div>

